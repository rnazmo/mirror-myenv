#!/usr/bin/env bash
set -eu

source "${MYENV_ROOT}/lib/util.bash"

setup_alacritty() {
    : # Phase 2: platform_install_alacritty + config + theme
}

setup_wezterm() {
    : # Phase 2: platform_install_wezterm + config
}
