#!/bin/bash
set -eu

source ${MYENV_DIR}/lib/util.bash

main() {
    # System update
    # TODO:

    # TODO:

    # Misc
    mkdir -p -v ~/bin
    mkdir -p -v ~/repos
    mkdir -p -v ~/workspace
    mkdir -p -v ~/workspace/sandboxes
    mkdir -p -v ~/workspace/temp
}

main
